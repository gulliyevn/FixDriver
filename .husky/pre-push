#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Запускаем проверки перед push
echo "🚀 Running pre-push checks..."

# Полный набор проверок
echo "🔍 Running full CI checks..."
npm run ci:full

echo "✅ All pre-push checks passed!"
