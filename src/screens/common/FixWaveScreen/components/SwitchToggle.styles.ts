import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  switchContainer: {
    width: 100,
    height: 30,
    borderRadius: 15,
    borderWidth: 1,
    alignItems: "center",
    justifyContent: "center",
    position: "relative",
  },
  switchThumb: {
    width: 50,
    height: 24,
    borderRadius: 12,
    position: "absolute",
  },
});
